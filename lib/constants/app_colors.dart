import 'package:flutter/material.dart';

/// アプリケーション全体で使用する色の定数
class AppColors {
  // 背景色
  static const Color backgroundColor = Color(0xFF3A3642);

  // アクセントカラー
  static const Color primaryColor = Color(0xFF5DADE2);

  // アクティブ/ハイライトカラー
  static const Color accentColor = Color(0xFFD946A6);

  // テキストカラー
  static const Color textPrimaryColor = Colors.white;
  static const Color textSecondaryColor = Colors.grey;

  // グリッドラインカラー
  static const Color gridLineColor = Colors.grey;

  // Private constructor to prevent instantiation
  AppColors._();
}
